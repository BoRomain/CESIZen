# CESIZen

## Installation

```bash
git clone https://github.com/BoRomain/CESIZen
cd CESIZen
npm install
```
Configurer le `.env` avec le `DATABASE_URL`, afin d'accéder à la base de données.

## Lancer le serveur
Lancer le serveur avec `npm run dev`